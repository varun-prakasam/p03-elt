packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.3.0", "<2.0.0"]
  # Moved from calogica to metaplane; the old namespace resolves but warns on every run.
  - package: metaplane/dbt_expectations
    version: [">=0.10.0", "<0.11.0"]
