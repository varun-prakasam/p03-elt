apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: p03-elt

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - configmap.yaml
  - cronjob.yaml

labels:
  - includeSelectors: false
    pairs:
      project_ref: p03-elt
